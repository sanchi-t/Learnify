import { TitleTruncatePipe } from './title-truncate.pipe';

describe('TitleTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
